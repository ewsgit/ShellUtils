const _dirname = process.cwd();
const _filename = process.argv[1];

const exp = {
    $user: process.env.USER,
    $home: process.env.HOME,
    $dirname: _dirname,
    $filename: _filename,
}

module.exports = exp;